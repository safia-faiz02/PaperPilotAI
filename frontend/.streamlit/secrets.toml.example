# Copy this file to secrets.toml (no ".example") in the same folder.
# Streamlit reads .streamlit/secrets.toml automatically — this is the
# idiomatic way to configure a Streamlit app, the equivalent of a .env
# file for the backend.

api_base_url = "http://localhost:8000"
